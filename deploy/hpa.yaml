apiVersion: autoscaling/v2beta2
kind: HorizontalPodAutoscaler
metadata:
  name: exporter
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: exporter
  minReplicas: 2
  maxReplicas: 10
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: 100
